import { createTheme } from "@mui/material";

export const MyThemes = createTheme({
  palette: {
    primary: {
      main: "#FF0A54",
    },
    secondary: {
      main: "#FF477E",
    },

    custom: {
      myBadge: "#FFFF00",
    },
  },
});